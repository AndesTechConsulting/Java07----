package org.andestech.learning.rfb18;import java.io.File;import java.io.FileWriter;import java.io.IOException;public class App {    public static void main( String[] args ) throws CreditInquiryException {        Customer first = new Customer(1, "Ivan", "Ivanov", 60000, 25, "GOOD");        Customer second = new Customer(2,"PETR", "Petrov", 20000, 15,"BAD");        String myCat = "C:\\Users\\Артем\\Documents\\";        File file1 = new File(myCat + "test.txt");        if (!file1.exists()) {            try {                file1.createNewFile();            } catch (IOException e) {                e.printStackTrace();            }        }        CreditInquiry first_credit = new CreditInquiry(10000);    //    CreditInquiry second_credit = new CreditInquiry(5000);        first_credit.doCreditInquiry(first);  //      second_credit.doCreditInquiry(second);        String strFile = first.toString();//        String sstrFile = second.toString();// Писатель        try (FileWriter fw = new FileWriter(file1)) {            //...            fw.write(strFile);            if(CreditInquiry.ACCEPT == true) {                CreditAccount crAcc = new CreditAccount(1, 10000, 12);                String crAccNew = crAcc.toString();                fw.write(crAccNew);            } else {                System.out.println("Не выдан");            } //           fw.write(sstrFile);        } catch (IOException e) {            e.printStackTrace();        }    }}